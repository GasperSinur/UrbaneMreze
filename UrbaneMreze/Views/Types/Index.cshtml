@model IEnumerable<UrbaneMreze.Models.Type>

@{
    ViewBag.Title = "Tipi";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Tipi</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TypeName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PinGuid)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TypeName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PinGuid)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TypeGuid }) |
            @Html.ActionLink("Details", "Details", new { id=item.TypeGuid }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TypeGuid })
        </td>
    </tr>
}

</table>
