@model IEnumerable<UrbaneMreze.Models.Spot>

@{
    ViewBag.Title = "Lokacije";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Lokacije</h2>

<p>
    @Html.ActionLink("Ustvari novo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SpotName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Longitude)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Latitude)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SpotName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Longitude)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Latitude)
        </td>
        <td>
            @Html.ActionLink("Uredi", "Edit", new { id=item.SpotGuid }) |
            @Html.ActionLink("Podrobnosti", "Details", new { id=item.SpotGuid }) |
            @Html.ActionLink("Izbriši", "Delete", new { id=item.SpotGuid })
        </td>
    </tr>
}

</table>
